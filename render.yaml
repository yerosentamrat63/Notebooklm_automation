services:
  - type: web
    name: notebooklm-automate
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NOTEBOOKLM_AUTH_JSON
        sync: false
